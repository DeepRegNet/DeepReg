language: python
python:
  - "3.7"      # current default Python on Travis CI
# Command to install dependencies
install:
  - pip install -e .
  - travis_wait 30 mvn install
script:
  - pytest
  - pytest --cov=./
