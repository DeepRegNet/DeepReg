dataset:
  dir:
    train: "data/test/nifti/paired/train"
    valid: "data/test/nifti/paired/test"
    test: "data/test/nifti/paired/test"
  format: "nifti"
  type: "paired" # paired / unpaired / grouped
  labeled: true
  moving_image_shape: [16, 16, 16]
  fixed_image_shape: [8, 8, 8]
